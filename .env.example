# OpenAI-compatible API config
# Copy this file to .env and fill in values. .env is not committed to git.

# Required: API key
OPENAI_API_KEY=sk-your-api-key-here

# Optional: OpenAI-compatible endpoint (omit to use OpenAI official API)
# OPENAI_API_BASE=https://your-api.example.com/v1

# Optional: Model name (default with base: gpt-4o)
# OPENAI_MODEL=gpt-4o

# Optional: OPENAI_MINIMAL=1 for small context models (e.g. 4096 tokens); uses no tools
# OPENAI_MINIMAL=1
